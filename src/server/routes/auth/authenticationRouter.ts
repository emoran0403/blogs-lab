import * as express from "express";
import db from "../../db";
import Validation from "../../Utils/DataValidation";

const authenticationRouter = express.Router();

//! Current route is somehting?!?!

export default authenticationRouter;
