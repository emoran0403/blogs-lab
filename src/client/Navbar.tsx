import * as React from "react";

const Navbar = () => {
  return (
    <>
      <div>
        <button className="btn btn-primary">Blogs</button>
        <button className="btn btn-primary">Authors</button>
        <button className="btn btn-primary">Logout</button>
      </div>
    </>
  );
};

export default Navbar;
