import * as React from "react";
import * as Types from "../types";

const Authors = (props: Types.AuthorsProps) => {
  return (
    <>
      <div>This is the Authors Component</div>
    </>
  );
};

export default Authors;
